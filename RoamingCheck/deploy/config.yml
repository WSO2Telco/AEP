server:
        applicationConnectors:
          - type: http
                # The port the application will listen on
            port: 8181
            bindHost: localhost
        adminConnectors:
          - type: http
                # The admin port
            port: 8282

redis:
  port: 6379
  host: localhost
  password: cY4L3dBf@mifenew
  timeout: 2000
  poolsize: 200


roamingCheck: